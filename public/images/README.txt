Drop your crest here as club-logo.png (referenced by the navbar/footer).
Player headshots and a default-avatar.png / default-player.png fallback
image can also live in this folder — paths are already wired in the
views/DB defaults (players.photo_url, users.avatar_url).
